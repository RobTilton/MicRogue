# Layout Interpretation Room DOTS
Updated: 2026-09-25

## Room Contract

- Room: `LayoutInterpretationRoom`
- Outcome: Build an encapsulated, generator-independent Layout Interpretation subsystem that accepts valid `MapData` plus a required architectural/site-purpose tag, claims useful semantic Zones from existing traversable geometry, records Zone metadata without placing content or changing map geometry, and exposes the completed result through a small public surface.
- Scope / prohibited territory: Define the architecture-purpose and Zone catalogs; analyze existing floor geometry; implement Zone claiming, ordered interpretation passes, coverage and fragment reconciliation; package interpretation metadata; expose and validate the subsystem; and maintain Room/current system documentation. Do not modify generated cell geometry or place doors, portals, stairs, traps, scenery, lighting, loot, actors, occupants, furnishings, decorations, or other content. Do not depend on generator internals. Occupant-family adaptation is a possible later system and is outside this Room. Production writes require a later Box-specific Alignment Check and Rob's confirmation before promotion begins.
- Acceptance: A caller can provide any valid generator-independent `MapData` and a supported required site-purpose tag; the subsystem produces coherent Zone IDs, architectural roles, tile membership, geometry and relationship metadata; universal and purpose-required Zone rules are applied in an explicit order; claimed tiles obey ownership invariants; coverage and irreducible leftovers are reported honestly; invalid or unsatisfiable inputs fail according to the approved contract; downstream systems can consume the result without knowing interpreter internals. Required automated checks pass, current-state/system documentation matches implementation, and human runtime/design acceptance remains recorded separately.
- Authority evidence: Rob established the initial system vision and four-stage traversal on 2026-09-25, then explicitly pivoted the catalog driver from occupant family to architecture/site purpose, directed preservation of family ideation in a non-authoritative pseudo-system document, and issued `Execute` for the pivot.
- Starting state: The Production Map Generation System is documented as returning detached `MapData` with `width`, `height`, and row-major `cells` using `ABYSS = 0`, `FLOOR = 1`, and `WALL = 2`. The Room scaffold and Rob's [`FirstPassIdeation.md`](Documents/FirstPassIdeation.md) exist. No Layout Interpretation implementation or Git checkpoint has been established; Git checkpoint is unknown.

## Current Traversal

- Last completed checkpoint: `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[PurposeContract]`
- Active/interrupted checkpoint: none
- Next eligible action: `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[InitialPurposes]` — choose the smallest concrete catalog representation that preserves the approved ordered requirements, multiplicities and relationships, then implement and validate the six entries without expanding their design.
- Human acceptance / Git checkpoint: Purpose Contract approved by Rob; implementation, final system acceptance, and Git checkpoint remain pending. Git checkpoint unknown.

## Mandatory Box List

| Checkpoint: [Room]+[Table]+[Box] | Responsibility | Depends on | Completion condition | Status | Outputs / evidence |
|---|---|---|---|---|---|
| `[LayoutInterpretationRoom]+[FamilyCatalog]+[FamilyContract]` | Former occupant-family-driven contract discovery. | none | Superseded by architecture/site-purpose interpretation before catalog approval or implementation. | superseded | Preserved direction: [`Tables/FamilyCatalog/README.md`](Tables/FamilyCatalog/README.md); ideation: [`../../AI_Facing_Documentation/SYSTEMS_DESCRIPTIONS_FOR_AI/Pseudo_System_Family_NOT_AUTHORITATIVE.md`](../../AI_Facing_Documentation/SYSTEMS_DESCRIPTIONS_FOR_AI/Pseudo_System_Family_NOT_AUTHORITATIVE.md). |
| `[LayoutInterpretationRoom]+[FamilyCatalog]+[InitialFamilies]` | Former initial occupant-family catalog. | `[LayoutInterpretationRoom]+[FamilyCatalog]+[FamilyContract]` | Superseded before execution because occupants do not define original architecture. | superseded | No implementation produced; candidate data preserved in the non-authoritative pseudo-system document. |
| `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[PurposeContract]` | Build a bounded site-purpose-and-space inventory; retain only distinctions that materially change architectural interpretation; classify survivors as required or optional; and derive the smallest justified purpose contract. | none | Rob approves the filtered purpose vocabulary, every retained space has an explicit architectural consequence, and the contract is justified by surviving requirements rather than assumed in advance. | complete | Approved contract: [`Tables/ArchitectureCatalog/PurposeContract.md`](Tables/ArchitectureCatalog/PurposeContract.md). Verified against the Architecture Catalog and DOTS on 2026-09-25; no runtime claims. |
| `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[InitialPurposes]` | Define and validate the initial architecture/site-purpose catalog against the approved contract. | `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[PurposeContract]` | Each approved purpose has complete, internally consistent requirements and references only identified Zone-role vocabulary. | planned | Planned: architecture-purpose catalog implementation/data, checks and synchronized reference. |
| `[LayoutInterpretationRoom]+[ZoneCatalog]+[ZoneContract]` | Define the general Zone specification: role identity, geometric limits, rotation, multiplicity, relationships, claim fallback and ownership guarantees. | `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[PurposeContract]` | Rob approves a Zone contract capable of expressing every role required by the purpose contract plus universal and fallback roles. | planned | Planned: `Tables/ZoneCatalog/` current-state/design reference. |
| `[LayoutInterpretationRoom]+[ZoneCatalog]+[UniversalZones]` | Define universal Spawn and Boss Zone rules, including valid footprint, route-relative placement and failure behavior, without placing gameplay objects. | `[LayoutInterpretationRoom]+[ZoneCatalog]+[ZoneContract]` | Universal Zone definitions are complete, mutually consistent and covered by catalog validation. | planned | Planned: universal Zone definitions and checks. |
| `[LayoutInterpretationRoom]+[ZoneCatalog]+[FamilyAndFallbackZones]` | Former family-required and fallback Zone catalog. | `[LayoutInterpretationRoom]+[FamilyCatalog]+[InitialFamilies]`, `[LayoutInterpretationRoom]+[ZoneCatalog]+[ZoneContract]` | Superseded before execution by architecture-purpose Zone definitions. | superseded | No implementation produced. |
| `[LayoutInterpretationRoom]+[ZoneCatalog]+[PurposeAndFallbackZones]` | Define approved purpose-required, optional and generic fallback Zone roles discovered by the Architecture Catalog. | `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[InitialPurposes]`, `[LayoutInterpretationRoom]+[ZoneCatalog]+[ZoneContract]` | Every architecture catalog reference resolves to a validated Zone definition and unsupported combinations fail clearly. | planned | Planned: Zone catalog implementation/data, checks and synchronized reference. |
| `[LayoutInterpretationRoom]+[ClaimAndZoning]+[GeometryAnalysis]` | Derive generator-independent traversability, connectivity, route-distance and available-geometry evidence from `MapData`. | `[LayoutInterpretationRoom]+[ZoneCatalog]+[ZoneContract]` | Analysis produces validated, non-mutating geometry facts needed by claiming, including a defined interpretation of longest navigable route. | planned | Planned: `Tables/ClaimAndZoning/` implementation, fixtures, checks and reference. |
| `[LayoutInterpretationRoom]+[ClaimAndZoning]+[ZoneClaim]` | Claim a bounded connected set of currently unclaimed floor tiles under an approved Zone specification. | `[LayoutInterpretationRoom]+[ZoneCatalog]+[PurposeAndFallbackZones]`, `[LayoutInterpretationRoom]+[ClaimAndZoning]+[GeometryAnalysis]` | Claiming respects geometry, rotation/size/tile limits and exclusive tile ownership; it either returns a complete valid claim or leaves state unchanged. | planned | Planned: claim implementation and focused tests. |
| `[LayoutInterpretationRoom]+[ClaimAndZoning]+[UniversalPass]` | Select the entrance end, claim the minimum valid Spawn Zone, and claim a valid Boss Zone at the farthest approved navigable endpoint. | `[LayoutInterpretationRoom]+[ZoneCatalog]+[UniversalZones]`, `[LayoutInterpretationRoom]+[ClaimAndZoning]+[ZoneClaim]` | Spawn and Boss claims satisfy their Zone contracts and route relationship, with explicit behavior when existing endpoint geometry is unsuitable. | planned | Planned: universal-pass implementation, fixtures and checks. |
| `[LayoutInterpretationRoom]+[ClaimAndZoning]+[FamilyAndCoveragePass]` | Former occupant-family-driven required and coverage passes. | `[LayoutInterpretationRoom]+[FamilyCatalog]+[InitialFamilies]`, `[LayoutInterpretationRoom]+[ClaimAndZoning]+[UniversalPass]` | Superseded before execution by the architecture-purpose pass. | superseded | No implementation produced. |
| `[LayoutInterpretationRoom]+[ClaimAndZoning]+[PurposeAndCoveragePass]` | Fulfill purpose-required Zones, then select optional/fallback Zones until the target or terminal condition is reached. | `[LayoutInterpretationRoom]+[ArchitectureCatalog]+[InitialPurposes]`, `[LayoutInterpretationRoom]+[ClaimAndZoning]+[UniversalPass]` | Ordered passes honor required relationships and multiplicities; coverage is calculated from traversable floor and termination cannot loop indefinitely. | planned | Planned: orchestration implementation, fixtures and checks. |
| `[LayoutInterpretationRoom]+[ClaimAndZoning]+[FragmentReconciliation]` | Resolve eligible tiny unclaimed fragments into adjacent Zones without violating Zone contracts and report irreducible leftovers. | `[LayoutInterpretationRoom]+[ClaimAndZoning]+[PurposeAndCoveragePass]` | No eligible fragment remains unnecessarily isolated; ownership, connectivity and coverage invariants pass; remaining unzoned tiles are classified honestly. | planned | Planned: reconciliation implementation, coverage evidence and synchronized reference. |
| `[LayoutInterpretationRoom]+[PackageAndSurface]+[InterpretationData]` | Define and implement detached interpretation metadata containing Zone identity, role, membership, geometry, relationships, coverage and unresolved leftovers. | `[LayoutInterpretationRoom]+[ClaimAndZoning]+[FragmentReconciliation]` | Output preserves the input map contract, represents all validated claims without duplicate ownership, and can be consumed without interpreter internals. | planned | Planned: `Tables/PackageAndSurface/` data contract, implementation and checks. |
| `[LayoutInterpretationRoom]+[PackageAndSurface]+[PublicEntryPoint]` | Expose the encapsulated call accepting valid `MapData` and a required supported architectural/site-purpose tag, with approved failure and randomness/determinism semantics. | `[LayoutInterpretationRoom]+[PackageAndSurface]+[InterpretationData]` | Public API is small, validates owned inputs, exposes no generator coupling or internal tuning knobs, and returns no partial result on failure. | planned | Planned: caller implementation, contract tests and synchronized reference. |
| `[LayoutInterpretationRoom]+[PackageAndSurface]+[SystemIntegration]` | Validate the composed subsystem across representative maps and approved architectural purposes, then prepare current Production-facing documentation and promotion evidence. | `[LayoutInterpretationRoom]+[PackageAndSurface]+[PublicEntryPoint]` | Approved integration matrix passes; known limits and human validation are recorded; documentation matches observed behavior; Production promotion occurs only under explicit scope authorization. | planned | Planned: integration evidence, Room current state and eventual system description. |

## Current References And Unresolved State

- [`Documents/FirstPassIdeation.md`](Documents/FirstPassIdeation.md) is Rob's initial concept draft and ideation evidence, not yet a final behavioral contract.
- `Tables/FamilyCatalog/`: preserved superseded occupant-driven direction; not an active dependency.
- `Tables/ArchitectureCatalog/`: active architecture/site-purpose language and initial purpose definitions.
- `Tables/ZoneCatalog/`: general, universal, purpose-specific and fallback Zone definitions.
- `Tables/ClaimAndZoning/`: geometry analysis, atomic claiming, ordered passes, coverage and fragment reconciliation.
- `Tables/PackageAndSurface/`: interpretation data, public entry point and composed validation.
- `References/`: reserved for Room-local current-state and task references.
- `Outputs/`: reserved for retained Room outputs that do not belong in a Table.
- Confirmed design direction: callers provide a required architecture/site-purpose tag; Layout Interpretation owns the meaning and application of that tag. It claims and describes what spaces were built for but never places their eventual contents.
- Confirmed initial purpose clamp: each current Map Generation Archetype has exactly two Layout Interpretation purposes—`DUNGEON`: `PRISON`, `CATACOMB`; `TOWER`: `MAGE_TOWER`, `GUARD_TOWER`; `CAVE`: `BURROW_NEST`, `MINE_SHAFT`. Later Boxes must not expand this approved list without new authority.
- Approved purpose requirements, multiplicities, claim order and relationships are authoritative in [`Tables/ArchitectureCatalog/PurposeContract.md`](Tables/ArchitectureCatalog/PurposeContract.md).
- Confirmed minimum Spawn footprint: `3x2`, with `2x3` treated as the same shape under 90-degree rotation.
- Confirmed catalog method: candidate architectural spaces survive only when their identity materially changes a layout decision, such as required presence, geometry, relationship, route position, multiplicity, accessibility, or spatial reservation. Occupant theming belongs to a later system.
- Purpose-aware optional/random selection is conditional, not presumed. Design it only if semantic reduction leaves enough meaningful non-universal optional roles to justify the mechanism; otherwise omit it.
- Occupant-family ideation is preserved in [`../../AI_Facing_Documentation/SYSTEMS_DESCRIPTIONS_FOR_AI/Pseudo_System_Family_NOT_AUTHORITATIVE.md`](../../AI_Facing_Documentation/SYSTEMS_DESCRIPTIONS_FOR_AI/Pseudo_System_Family_NOT_AUTHORITATIVE.md). It is not runtime authority and does not belong to this Room's active traversal.
- Proposed but not yet approved as final contracts: exact Zone geometry; the final generic coverage target; exact fragment thresholds; output attachment versus detached representation; deterministic/randomness guarantees; and unsatisfied-required-Zone failure behavior.
- Existing Map Generation documentation is an input contract reference, not authority to modify that system.
- Output disposition: retain Room documents and working outputs until explicit promotion or disposition authority is granted.
